import { FaSignal } from "react-icons/fa";

const IconBarchart = () => {
  // apply a transform to flip icon horizontally
  return <FaSignal style={{ transform: "scaleX(-1)" }} />;
};

export default IconBarchart;
